version: "3.8"
services:
  api:
    build:
      context: .
      args:
        NODE_ENV: production
    command: npm start

  nginx:
    volumes:
      - ./files/nginx/prod.conf:/etc/nginx/conf.d/default.conf:ro
    ports:
      - "80:80"
      - "443:443"